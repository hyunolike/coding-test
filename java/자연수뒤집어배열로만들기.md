## 자연수뒤집어배열로 만들자
```java
public int[] solution(long n){
  String a = "" + n;  // Integer.toString() 같은 역할
  
  int[] answer = new int[a.length()];
  int cnt = 0;
  
  while(n > 0){
    answer[cnt] = (int)(n%10);
    n /= 10;
    cnt++;
  }
  return answer;
}
```
